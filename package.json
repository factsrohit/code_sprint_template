{
  "name": "code-sprint-server",
  "version": "1.0.0",
  "description": "Code Sprint June 2026 - Backend",
  "main": "server/server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "node -e \"const v=process.versions.node.split('.')[0]; if (+v < 24) { console.error('Node 24+ is required.'); process.exit(1); }\"",
    "postinstall": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/server.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev": "nodemon server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "@libsql/client": "^0.15.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
